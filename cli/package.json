{
  "name": "ccusage-cloud",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "ccusage-cloud": "bin/ccusage-cloud.js" },
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "valibot": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
